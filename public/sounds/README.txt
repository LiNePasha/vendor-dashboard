Place your custom notification sound here as `notify.mp3`.

Recommended formats: .mp3 (most compatible), .ogg (fallback)
Path used by the app: /sounds/notify.mp3

Tips:
- Keep it short (0.3s - 1.5s) and punchy
- Normalize the volume (around -6 dB)
- Avoid very low frequencies (<200 Hz) on mobile
