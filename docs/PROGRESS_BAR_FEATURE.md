# ✅ إضافة Progress Bar لرفع الفاتورة

## 🎯 الميزات الجديدة

### 1. Progress Bar مرئي
- **نسبة مئوية:** يعرض كام فالمية اتعمل (0% → 100%)
- **عداد:** يعرض (3 / 10) مثلاً
- **شريط تقدم:** Visual progress bar بتدرج لوني

### 2. Visual Indicators لكل منتج
- **⏳ أثناء الرفع:** المنتج الحالي يتظلل بالأزرق مع أيقونة ⏳ دوارة
- **✓ بعد الرفع:** السطر يتحول أخضر مع علامة ✓
- **تعطيل Inputs:** أثناء الرفع، كل الـ inputs تتقفل

### 3. رفع تدريجي (Sequential Upload)
- المنتجات بترفع **واحدة ورا التانية** (مش كلها مرة واحدة)
- تأخير 100ms بين كل منتج عشان المستخدم يشوف التقدم
- لو منتج فشل، بيكمل في اللي بعده

## 📊 مثال على الـ UI

### أثناء الرفع:
```
جاري الاستيراد... (3 / 10)         30%
[████████████░░░░░░░░░░░░░░░░] 30%

# | المنتج        | SKU  | الكمية | السعر | إجراء
1 | منتج 1 ✓      | A001 | 5      | 100   | ✓ تم      [أخضر]
2 | منتج 2 ✓      | A002 | 3      | 150   | ✓ تم      [أخضر]
3 | منتج 3 ⏳     | A003 | 2      | 200   | 🗑️        [أزرق - يتحرك]
4 | منتج 4        | A004 | 10     | 50    | 🗑️        [رمادي]
...
```

### بعد الانتهاء:
```
تم استيراد 10 من 10 منتج بنجاح ✅
```

## 🔧 التفاصيل التقنية

### State Management:
```javascript
const [importProgress, setImportProgress] = useState({ 
  current: 0,  // المنتج الحالي
  total: 0     // إجمالي المنتجات
});

const [importedProducts, setImportedProducts] = useState(new Set()); 
// Set of indices للمنتجات اللي اترفعت
```

### Upload Logic:
```javascript
for (let i = 0; i < products.length; i++) {
  // Update progress
  setImportProgress({ current: i + 1, total: products.length });
  
  // Upload product
  const res = await fetch('/api/warehouse/bulk-create', { ... });
  
  // Mark as imported
  setImportedProducts(prev => new Set([...prev, i]));
  
  // Small delay for visual feedback
  await new Promise(resolve => setTimeout(resolve, 100));
}
```

### Visual States:
```javascript
const isImported = importedProducts.has(index);
const isCurrentlyImporting = importing && importProgress.current === index + 1;

// Row styling
className={`
  ${isImported ? 'bg-green-50' : ''} 
  ${isCurrentlyImporting ? 'bg-blue-50 animate-pulse' : ''}
`}
```

## 🎨 Styling

### Progress Bar:
- **Container:** رمادي فاتح
- **Fill:** تدرج من بنفسجي لأزرق
- **Animation:** `transition-all duration-300 ease-out`

### Table Rows:
- **Default:** `hover:bg-gray-50`
- **Importing:** `bg-blue-50 animate-pulse`
- **Imported:** `bg-green-50`

### Icons:
- **✓ (تم):** أخضر في آخر عمود
- **⏳ (جاري):** أزرق دوار في أول عمود
- **🗑️ (حذف):** أحمر، يتعطل أثناء الرفع

## 🚀 الاستخدام

1. ارفع فاتورة
2. انتظر استخراج المنتجات
3. راجع البيانات في الجدول
4. اضغط "استيراد X منتج"
5. **شاهد التقدم:**
   - شريط التقدم يتحرك من 0% → 100%
   - كل منتج يترفع يتظلل بالأزرق ثم الأخضر
   - ✓ تظهر على المنتجات المرفوعة

## ✅ المزايا

- **Feedback فوري:** المستخدم يعرف إن في حاجة بتحصل
- **Transparency:** يعرف أي منتج اترفع وأي لسه
- **Error Handling:** لو منتج فشل، بيكمل في باقي المنتجات
- **User Experience:** أفضل من loading spinner ثابت

## 🎯 النتيجة

بدل ما كان loading indicator واحد، دلوقتي:
- **Progress bar** بنسبة مئوية
- **Visual feedback** لكل منتج
- **Real-time tracking** للعملية
- **تجربة مستخدم احترافية** 🎉
