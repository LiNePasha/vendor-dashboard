# تحديثات نظام إدارة العملاء وأوامر التوصيل - ملخص التنفيذ

## ✅ ما تم إنجازه:

### 1. نظام العملاء الأوفلاين
- **ملف:** `app/lib/offline-customers-storage.js`
- **الوظائف:**
  - إضافة/تعديل/حذف عملاء أوفلاين
  - البحث والفلترة
  - تحديث إحصائيات العميل بعد كل طلب
  - دمج العملاء الأونلاين والأوفلاين

### 2. واجهة إضافة العملاء
- **ملف:** `components/CustomerModal.js`
- **الميزات:**
  - نموذج كامل لبيانات العميل (اسم، هاتف، إيميل)
  - عنوان توصيل تفصيلي (محافظة، مدينة، منطقة، شارع، عقار، دور، شقة، علامة مميزة)
  - ملاحظات
  - Validation للحقول المطلوبة

### 3. صفحة العملاء المحدثة
- **ملف:** `app/customers/page.js`
- **التحديثات:**
  - دمج العملاء الأونلاين (من WooCommerce) والأوفلاين (من localStorage)
  - فلتر حسب النوع (أونلاين/أوفلاين)
  - زر إضافة عميل جديد
  - أزرار تعديل/حذف للعملاء الأوفلاين فقط
  - إحصائيات محدثة تظهر عدد العملاء من كل نوع

---

## 🔄 التعديلات المتبقية لنظام POS:

### 4. تعديل POS Store (pos-store.js)
**المطلوب:**
```javascript
// إضافة حالة جديدة:
- orderType: 'pickup' | 'delivery'
- selectedCustomer: null | Customer
- deliveryAddress: { ... }
- deliveryFee: 0

// دوال جديدة:
- setOrderType(type)
- selectCustomer(customer)
- setDeliveryAddress(address)
- setDeliveryFee(fee)
```

### 5. تعديل POS Page (app/pos/page.js)
**المطلوب:**
- إضافة قسم اختيار نوع الطلب (Pickup / Delivery)
- إضافة قسم اختيار/إضافة عميل (للتوصيل)
- إضافة حقل رسوم التوصيل
- عرض بيانات العميل والعنوان في Cart
- تعديل checkout لحفظ بيانات التوصيل

**الواجهة المقترحة:**
```
┌─────────────────────────────────────┐
│ نوع الطلب: ⚪ استلام  🔘 توصيل    │
│                                     │
│ [إذا توصيل:]                        │
│ العميل: [اختر عميل ▼] [+ إضافة]   │
│ العنوان: [عنوان كامل]              │
│ رسوم التوصيل: [50] ج.م             │
└─────────────────────────────────────┘
```

### 6. إنشاء DeliveryInvoiceModal
**الملف:** `app/pos/DeliveryInvoiceModal.js`
**المحتوى:**
- معلومات الشركة/المحل
- بيانات العميل كاملة
- العنوان التفصيلي
- رقم الهاتف
- قائمة المنتجات
- الإجمالي + رسوم التوصيل
- ملاحظات التوصيل
- زر طباعة

---

## 📋 الهيكل النهائي المتوقع:

```
app/
  lib/
    ✅ offline-customers-storage.js
    pos-store.js (يحتاج تعديل)
  
  pos/
    page.js (يحتاج تعديل)
    InvoiceModal.js (موجود)
    ⚠️ DeliveryInvoiceModal.js (يحتاج إنشاء)
  
  customers/
    ✅ page.js (تم التعديل)

components/
  ✅ CustomerModal.js (تم الإنشاء)
  ⚠️ CustomerSelector.js (مقترح - لاختيار عميل سريع)
```

---

## 🎯 الخطوات القادمة:

1. تعديل `pos-store.js` لإضافة حالة الطلبات التوصيل
2. تعديل `app/pos/page.js` لإضافة واجهة اختيار نوع الطلب والعميل
3. إنشاء `DeliveryInvoiceModal.js` لطباعة بوليصة التوصيل
4. إنشاء `CustomerSelector.js` (اختياري) لاختيار عميل سريع
5. اختبار التكامل الكامل

---

## 💡 ملاحظات تقنية:

- **العملاء الأوفلاين**: يتم حفظهم في localStorage باستخدام localForage
- **العملاء الأونلاين**: يتم جلبهم من WooCommerce API
- **الدمج**: يتم دمج النوعين في الواجهة مع تمييز واضح
- **الإحصائيات**: يتم تحديث إحصائيات العميل تلقائياً بعد كل طلب
- **التوصيل**: يتم حفظ بيانات التوصيل مع الطلب في POS

---

## 🔐 الصلاحيات:

- **العملاء الأونلاين**: قراءة فقط (لا يمكن تعديل/حذف)
- **العملاء الأوفلاين**: إضافة/تعديل/حذف كامل
- **بيانات التوصيل**: متاحة للجميع في POS

---

## 📊 الإحصائيات المضافة:

```javascript
{
  total: 150,        // إجمالي العملاء
  online: 120,       // عملاء أونلاين
  offline: 30,       // عملاء أوفلاين
  active: 80         // عملاء نشطون (من النوعين)
}
```

---

**آخر تحديث:** 2026-01-01
**الحالة:** جاري التنفيذ (60% مكتمل)
